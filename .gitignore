# Environment and secrets — NEVER share these
.env
env.txt
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Archives (may contain keys)
*.zip

# Benchmark and A/B test output — generated data, not source
comparison/benchmark_*.json
comparison/benchmark_*.jsonl
comparison/ab_*.json
comparison/ab_*.jsonl
comparison/parkinglot/
GPT\ comparison/results_*.json

# GPT comparison — standalone prototype, superseded by comparison/ and app/
GPT\ comparison/

# Article / documentation directory — drafts, PDFs, docx, diagrams
# These are not part of the runnable codebase
article/

# Competitive research spreadsheet — internal, not for public repo
Search_API_Use_Case_Competitive_Audit.xlsx

# Regression test suite — local dev tooling, not part of the published codebase
regression/

# MCP server config — local dev tooling, machine-specific paths
**/mcp_config.json

# Claude Code project config — local dev tooling, not project config
.claude/
